#!KAMAILIO

# --- listen locally on 5060/udp
listen=udp:127.0.0.1:5060

# --- modules
loadmodule "sl.so"
loadmodule "tm.so"
loadmodule "rr.so"
loadmodule "maxfwd.so"
loadmodule "textops.so"
loadmodule "pv.so"
loadmodule "siputils.so"
loadmodule "xlog.so"
loadmodule "lwsc.so"
loadmodule "rtpengine.so"

# --- module params
modparam("tm", "fr_timer", 30000)
modparam("tm", "fr_inv_timer", 120000)
modparam("rr", "enable_full_lr", 1)

modparam("lwsc", "verbosity", 1)
# point to your RTPengine emulator control socket
modparam("rtpengine", "rtpengine_sock", "ws://127.0.0.1:2225")
modparam("rtpengine", "wsapi", "lwsc")

# --- routing
request_route {
    if (!mf_process_maxfwd_header("10")) {
        sl_send_reply("483","Too Many Hops"); 
        exit; 
    }

    # in-dialog requests
    if (has_totag()) {
        if (loose_route()) { route(RELAY); }
        exit;
    }

    if (is_method("INVITE")) {
        t_newtran();
        t_on_reply("MANAGE_REPLY");

        # call rtpengine_offer on initial INVITE with SDP
        if (has_body("application/sdp")) {
            $var(flags) = "replace-origin replace-session-connection";
            if (!rtpengine_offer($var(flags))) {
                xlog("L_ERR", "rtpengine_offer failed\n");
                sl_send_reply("488","Not Acceptable Here");
                exit;
            }
        }

        record_route();

        $du = "sip:1000@127.0.0.1:5070";   # SIPP UAS

        route(RELAY);
        exit;
    }

    if (is_method("ACK|BYE|CANCEL|INFO|PRACK|UPDATE|REFER|OPTIONS")) {
        route(RELAY);
        exit;
    }

    sl_send_reply("405", "Method Not Allowed");
    exit;
}

route[RELAY] {
    if (!t_relay()) {
        sl_reply_error();
    }
    exit;
}

# Call rtpengine_answer on provisional/OK replies that carry SDP
onreply_route[MANAGE_REPLY] {
    if (t_check_status("180|183|200") && has_body("application/sdp")) {
        $var(answer_flags) = "replace-origin replace-session-connection";
        if (!rtpengine_answer($var(answer_flags))) {
            xlog("L_ERR", "rtpengine_answer failed\n");
        }
    }
}

